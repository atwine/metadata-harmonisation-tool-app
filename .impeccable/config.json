{
  "detector": {
    "ignoreRules": [],
    "ignoreFiles": [],
    "ignoreValues": [
      {
        "rule": "side-tab",
        "value": "*",
        "files": [
          "src/routes/initialise.tsx"
        ],
        "createdAt": "2026-08-31T11:27:37.240Z",
        "reason": "Pre-existing border-l-4 status-banner pattern used consistently across this file (connection status, AI-not-configured, already-initialised, run-result banners) since before this session; not touched by the delete-confirmation-modal commit that triggered this hook. Out of scope for that task — a deliberate app-wide restyle wasn't requested."
      },
      {
        "rule": "overused-font",
        "value": "inter",
        "createdAt": "2026-09-11T10:31:18.831Z",
        "reason": "pre-existing app-wide typography set in __root.tsx before this feature branch; not introduced by the eval-build work, out of scope to change here"
      },
      {
        "rule": "side-tab",
        "value": "*",
        "files": [
          "src/routes/eval-questionnaire.tsx"
        ],
        "createdAt": "2026-09-11T10:31:25.650Z",
        "reason": "matches the existing border-l-4 status-banner pattern already used app-wide (e.g. src/routes/initialise.tsx), predates this branch, not a new choice introduced here"
      },
      {
        "rule": "side-tab",
        "value": "*",
        "files": [
          "src/components/eval/SubmitReportButton.tsx"
        ],
        "createdAt": "2026-09-11T11:14:35.019Z",
        "reason": "matches the existing border-l-4 warning-banner pattern already used app-wide (e.g. src/routes/initialise.tsx's AI-not-configured notice), not a new choice introduced here"
      },
      {
        "rule": "side-tab",
        "value": "*",
        "files": [
          "src/components/eval/EvalDisclosure.tsx"
        ],
        "createdAt": "2026-09-21T13:08:28.729Z",
        "reason": "Assistant decision, not user-confirmed: the new 'written answers will be public' callout deliberately reuses the app's existing border-l-4 status-banner pattern (initialise.tsx, eval-questionnaire.tsx, SubmitReportButton.tsx, already suppressed for the same reason) so it reads as one of the app's warnings; a one-off restyle was not requested."
      }
    ]
  }
}
